# Copy this to .env.local for local overrides (optional - Vite proxy handles dev)
# Set this in Vercel Dashboard → Project Settings → Environment Variables
VITE_API_URL=https://backend-name.onrender.com
